import { StyleSheet } from "react-native";

/*
  100% manual
*/

export const loginStyles = StyleSheet.create({
    hint: {
        color: "#aaa",
        fontSize: 12,
        marginTop: 4
    },
    error: {
        color: "#ff6b6b", 
        marginTop: 12, 
        textAlign: "center"
    }
});