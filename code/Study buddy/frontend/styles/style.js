import { StyleSheet } from "react-native";

// general stylesheet used across the app
export const styles = StyleSheet.create({

// style for buttons
navigationButton: {
  backgroundColor: 'rgba(255, 0, 144, 1)',
  color: '#ffffffff',
  fontSize: 30,
  cursor: 'pointer',
  opacity: 0.95,
  width: 300,
  height: 100
}

});